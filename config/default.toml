[discord]
# トークンは.envファイルから読み込みます
# ここには何も設定しません

[rss]
feed_url = "https://b.hatena.ne.jp/t/ai?mode=rss"
poll_interval_seconds = 300  # 5分ごとにチェック

[hatena]
thresholds = [1, 5, 20]
count_api_timeout_seconds = 10
candidate_retention_days = 7

[[notification.channels]]
threshold = 1
channel_id = "replace_me_1"

[[notification.channels]]
threshold = 5
channel_id = "replace_me_5"

[[notification.channels]]
threshold = 20
channel_id = "replace_me_20"

[state]
file_path = "data/articles.json"
skip_existing_on_first_run = true
